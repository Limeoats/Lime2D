# Enable C++ support
language: cpp
# Compiler selection
compiler:
  - clang
  - gcc
# Build steps
install:
  - sudo apt-get install -qq g++-5  
  - sudo apt-get install libsfml-dev
script:
  - mkdir build
  - cd build
  - cmake .. && make
